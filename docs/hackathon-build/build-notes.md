# Guided Build Notes

## 2026-08-29 — Guided onboarding started

- The project owner explicitly chose the optional guided build path after a direct implementation and Devpost-compliance pass.
- Historical baseline, superseded by the approved Scope and PRD: PaperPilot was initially going to demonstrate a browser agent staging exact webpage evidence for explicit human review, with source, agent, and human authority kept visibly separate.
- Existing product work is not being discarded. The guided documents will refine and govern the remaining WebMCP build.
- The repository already contains a strict capture contract, a capability-detected browser adapter, a focused WebMCP scope, a Devpost compliance matrix, a judge guide, and a fail-closed readiness audit.
- Active shaping moment: “Use guided build path so that we can develop better from now on.”
- Onboarding Round 1 completed. The project owner described PaperPilot as an agentic WebMCP application for overcoming scientific-literacy and specialist-language barriers.
- Core user gesture: upload a paper, highlight an unfamiliar passage or figure, and ask the agent for an accessible explanation.
- Core trust requirement: the explanation must remain grounded in the supplied material, expose provenance, and make agent hallucination or unsupported interpretation detectable.
- Technical calibration: the project owner is an advanced technical contributor, prefers enough planning to establish boundaries, and wants to build aggressively afterward; personal background details are omitted from the public repository.
- Active shaping moment: the vision expanded from webpage-passage capture alone to scientific-literacy explanations over uploaded paper text and figures.
- Onboarding sharpening Round 2 completed.
- Target user: a general reader at approximately undergraduate level reading an early difficult scientific paper, with basic prior knowledge but without specialized fluency.
- Canonical product loop accepted: upload → Reader → select text or figure → browser-agent explanation → provenance inspection → save or reject.
- Architecture direction accepted: the WebMCP-capable browser agent performs the explanation; PaperPilot exposes bounded selection-reading and proposal-writing tools and owns provenance, review, and persistence.
- Active shaping moment: the project owner made text and figures equal first-class requirements rather than treating figure understanding as a stretch goal.
- Onboarding creative Round 3 completed.
- Experience direction: hip, easy to use, and accessibility-first; avoid an intimidating institutional-research aesthetic.
- Agent voice: a supportive research mentor.
- Provenance metaphor: an evidence trail.
- Product inspiration: Notion-like clarity and progressive disclosure.
- Signature visual accepted: source passage or figure on the left, accessible explanation in the center, source → agent → human evidence trail on the right.
- Onboarding complete with all three rounds answered; guided Scope interview started next.

## 2026-08-29 — Scope interview

- Brain dump confirmed: PaperPilot should turn every word, line, equation, passage, figure, and figure region into an interactive teaching surface so readers do not have to leave the paper and reconstruct missing knowledge across unrelated searches.
- Teaching focus: jargon and definitions, prerequisite concepts, difficult technical and mathematical reasoning, within-paper synthesis, figure understanding, and screen-reader descriptions.
- Inspiration reaction: emphasize Explainpaper-like highlighting and direct interaction, Perplexity-like source proximity with stronger exact provenance, and Notion-like calm navigation.
- Time ruler: target feature completion by Tuesday, 2026-09-01; reserve the rest of the submission window for release work.
- Provenance decision: combine paper-grounded evidence, clearly labeled mentor background knowledge, and cited external authoritative sources without merging their authority.
- Figure decision: both full-figure and arbitrary rectangular-region selection are in scope.
- Reliability decision superseded by participant: curated or deterministic paper content is not acceptable; arbitrary user-uploaded PDFs are a core requirement.
- Active shaping moment: “We can’t have deterministic and we have to have it working across arbitrary PDFs.”
- The remainder of the proposed cut line is approved, including the canonical structured mentor card, accessibility essentials, friendly `Save to notes` / `Discard` UI language, and the named deferred capabilities.
- Arbitrary-PDF contract approved: valid non-encrypted bounded PDFs are rendered; exact embedded text is retained when reliable; weak-text/scanned documents retain visual regions with OCR/vision derivation labeled; corrupt, encrypted, unsupported, and oversized files fail explicitly.
- Participant approved the remainder of the cut line and chose to write the formal Scope without an additional deepening round.
- Scope deepening count: 0.
- Created `docs/hackathon-build/scope.md` as the canonical hackathon product scope.
- Marked the earlier webpage-first `docs/WEBMCP-PROVENANCE-SCOPE.md` as an architecture reference superseded for current product priorities.
- Scope handoff: next guided stage is PRD.

## 2026-08-29 — PRD interview

- Formal Scope accepted and carried forward without reopening its product boundaries.
- PRD focus: first-run behavior, upload and processing states, Reader selection behavior, WebMCP mentor interaction, structured explanation review, evidence-trail comprehension, persistence, accessibility, and testable user-visible outcomes.
- First-run recommendation approved: a calm empty library with one prominent upload action, concise product promise, supported-file/privacy information, no demo content, and recent-paper continuation for returning users.
- Upload recommendation approved: human-readable processing states, early Reader access when rendered pages are available, visual-region interaction before text readiness when necessary, and specific recoverable failure guidance.
- Reader recommendation approved: direct text highlighting, explicit visual-region mode, persistent selection outline, preflight disclosure of what will be shared, and equivalent keyboard/screen-reader paths.
- WebMCP handoff recommendation approved: `Ready for your research mentor`, a suggested request, the browser agent's normal conversation surface, and visible selection-ready / mentor-reading / explanation-ready states.
- PRD epics accepted: start reading, understand processing, select difficult material, ask the WebMCP mentor, receive a structured explanation, follow the evidence, and keep only useful results.
- Explanation arrival approved: keep the paper and selection in place, announce readiness, and let the user explicitly move focus to the explanation.
- Follow-up behavior approved: each mentor response is a separate staged proposal with its own evidence trail; no silent overwrites.
- Save behavior approved: preserve the mentor response unchanged and allow an optional, separately labeled `My takeaway`; saving never requires user-authored text.
- Evidence disclosure approved: a simple paper → mentor → user trail by default with technical coordinates, authorities, citations, WebMCP calls, timestamps, and digests behind `Show evidence details`.
- Edge behavior approved: an in-flight request remains bound to its frozen original selection; later selections and results stay separate.
- Weak-text behavior approved: preserve visual-region use, label OCR/vision wording as derived, and disable explanation only when the page cannot render.
- WebMCP failure behavior approved: distinguish unavailable, registration failure, cancellation, invalid response, and interruption; preserve the selection; never fabricate native success; label any local review fallback throughout.
- External-source behavior approved: retain citation warnings, never merge a citation into paper evidence, open links safely, and allow an informed save without claiming the citation proves truth.
- Time guard retained: sharing/export, collaboration, bulk generation, and cross-paper comparison remain outside the Tuesday build.
- Mandatory PRD beats complete; optional deepening choice pending.
- PRD deepening count: 0. Mandatory behavior interview complete.
- Participant chose one PRD deepening round.
- Explanation-depth decision: default undergraduate response plus `Make it simpler`, `Go deeper`, and `Show the math`; each follow-up is a separate response bound to the same source rather than an overwrite.
- Synthesis decision: genuine within-article synthesis is first-class, including contextual single-selection explanation and deliberate synthesis across multiple user-selected passages, equations, or figures from the same paper.
- Persistence decision: restore the last paper/page, saved notes, valid pending responses, evidence trails, and `My takeaway`; do not restore unfinished selections or requests that never yielded a valid response.
- Nonvisual-figure decision: offer current-page description, identified figure/caption choices, and labeled-item questions without requiring a visual rectangle.
- Devpost wow decision: the hero is visible real WebMCP activity—registered tools plus PaperPilot-observed bounded source-read and structured-stage callbacks—and the resulting provenance trail. Tool discovery is shown only when a supported client exposes it observably; private agent reasoning is never claimed. The figure explanation demonstrates value but is not the sole hero.
- PRD deepening count: 1. Participant requested exactly one additional round and confirmed its transcription; formal PRD generation started.
- Created `docs/hackathon-build/prd.md` with 10 product epics, 30 stable user stories, screen-observable acceptance criteria for every story, a failure/edge-case matrix, release proof matrix, and guarded submission claims.
- Reconciled the Scope with the later approved decisions: deliberate bounded same-paper multi-selection synthesis is first-class, mentor responses remain unchanged, and user interpretation lives only in the separate optional `My takeaway`.
- Tightened the public PDF claim from universal/arbitrary success to paper-agnostic support for previously unseen user uploads that meet published admission limits, with per-page exact/visual/unavailable capability.
- PRD independent reviews covered acceptance completeness, accessibility, visible WebMCP activity, scope discipline, and false-claim boundaries; their highest-value findings were incorporated.
- PRD handoff: next guided stage is the technical Spec.

## 2026-08-29 — PRD approval and repository-requirements reconciliation

- The project owner confirmed that the final PRD direction is accurate.
- Formal PRD remains the approved 10-epic, 30-story user-behavior contract; the technical Spec is the next guided stage.
- Replaced the obsolete deterministic webpage-fixture requirement in the Devpost manifest and readiness audit with the canonical paper-agnostic admitted-PDF contract.
- Repository gates now require exact-text, page-region, whole-figure, figure-region, and bounded same-paper synthesis proof plus bounded PDF-source read and structured mentor-stage WebMCP capabilities.
- Added explicit release-verification gates for replaceable previously unseen PDFs, observable WebMCP activity, keyboard and screen-reader use, and the persistent **Local review—WebMCP was not invoked** fallback label.
- Rewrote the judge guide and README challenge framing around the scientific-literacy Reader while preserving the earlier webpage-capture work only as a superseded architecture reference.

## 2026-08-29 — Technical Spec interview

- The project owner advanced naturally with “Next”; the guided technical Spec stage is active.
- Repository survey confirmed a reuse-first base: Next.js 16, React 19, TypeScript, Better Auth, Prisma/PostgreSQL, private PDF upload and custody, isolated Poppler validation/extraction workers, an authenticated text Reader, immutable text anchors, and existing provenance/review primitives.
- Current extraction is embedded-text only. It does not render PDF pages, produce OCR, or retain visual crops.
- Current WebMCP work is an older webpage-evidence adapter and contract; it is a useful registration/test pattern but is not the canonical PDF mentor integration.
- Official current WebMCP references confirm the imperative `document.modelContext.registerTool` surface, AbortSignal-owned registration lifetime, `readOnlyHint`, `untrustedContentHint`, and an execution cancellation signal. Portable image-specific tool-result semantics remain unsettled, so the initial architecture must not depend on them.
- Initial recommendation batch: keep the existing TypeScript/PostgreSQL stack; let the browser agent remain the only explanation engine; render validated PDFs client-side with PDF.js while retaining Poppler chunks as exact-text authority; expose selected visual content in the current page for browser-agent observation; and deploy the existing filesystem/worker topology on one Linux Docker host instead of forcing it into serverless infrastructure.
- The project owner approved the full initial recommendation batch without modification.
- Stack boundary locked: Next.js 16, React 19, TypeScript, Better Auth, Prisma/PostgreSQL, and the existing validation/extraction services remain; PaperPilot adds no separate Python API and no in-product/server-side explanation model. The browser's WebMCP agent is the only mentor explanation engine.
- Rendering boundary locked: add a pinned `pdfjs-dist` version for client page rendering and an authenticated endpoint for the exact admitted original PDF. Poppler chunks remain exact-text authority; PDF.js page pixels and retained crop artifacts provide visual authority. Visual snapshots bind document digest, page, rotation, normalized rectangle, renderer recipe/version, full-context digest, and crop digest; a failed integrity recheck displays `Source incomplete`.
- Figure handoff locked with a release gate: the selected crop remains prominently available in a named semantic `Selected source` region, while the read tool supplies the bounded locator, authority, context, and digests. Because portable image-specific WebMCP results remain unsettled, successful visual consumption must be verified in the named supported judge client; unsupported clients state the limitation and never count figure handling as native WebMCP proof. No second vision model is introduced.
- Deployment locked: one public HTTPS Linux VPS using Docker Compose, a TLS reverse proxy, Next.js, PostgreSQL with a persistent volume, validation/extraction workers and isolated services, and a private durable PDF/artifact volume. Managed database, object-storage, serverless, crawler, Zotero, and broader networking migrations remain post-hackathon work behind stable domain contracts.
- The project owner approved Architecture Round 2 without changes.
- Library boundary locked: an authenticated upload creates a provisional upload-backed library paper with a filename-derived display title and no invented bibliography; validation and extraction gates remain authoritative, while Zotero/crawler reconciliation is deferred behind the same paper contract.
- Reader boundary locked: maintain separate page and exact-text surfaces over the admitted PDF. PDF.js supplies page/figure/region interaction, Poppler admissions supply replayable exact text, and neither authority is silently promoted into the other.
- WebMCP tool surface locked: add Reader-specific `paperpilot.read_active_paper_sources` and `paperpilot.stage_mentor_explanation`; keep the older webpage-capture tools semantically separate; never expose save, discard, approve, or verify as agent-callable tools.
- Mentor persistence boundary locked: add `ReaderProgress`, `PaperMentorRequest`, `PaperMentorSourceItem`, `PaperMentorProposal`, `PaperMentorActivityEvent`, and `PaperMentorDecision`; reuse `EvidenceNote` only as a projection created by an explicit human Save.
- Provenance/activity boundary locked: client-observed registration, server-observed read/stage, and human Save/Discard remain distinct. Registration is not claimed as discovery, callback delivery is not claimed as agent reasoning, and local review creates no fabricated WebMCP events.
- Tuesday engineering ceilings locked as configurable safeguards: at most eight same-document items, 50,000 UTF-8 exact-text bytes, two visual items, one active frozen handoff per Reader tab, a configured visual-payload ceiling, and no cross-paper synthesis.
- The project owner approved the final mandatory architecture round, including the annotated repository map, REST surface, primary data lifecycle, and implementation dependency order.
- Visual-integrity correction approved: a saved visual source retains the exact original PDF.js-produced artifact bytes and the server-recomputed digest of those received bytes, bound to admitted PDF digest, page, rotation, normalized geometry, renderer version, and recipe. Reopening verifies stored artifact integrity and source binding; it does not require a fresh cross-browser canvas render to reproduce an identical pixel digest.
- Figure-handoff correction approved: the portable read-tool contract does not require base64 or a nonstandard multimodal result. Selected pixels remain prominently visible in the semantic `Selected source` region; an inline-image result is permitted only as a named-client-tested compatibility enhancement and never as a general WebMCP claim.
- Persistence refinement approved and supersedes the earlier six-record simplification: use `ReaderProgress` plus six mentor/provenance records—`ReaderSourceSet`, `ReaderSourceItem`, `MentorExchange`, `MentorActivityEvent`, `MentorProposal`, and `MentorDecision`. Separate immutable source sets allow follow-up exchanges to reuse the exact same source without copying artifacts or conflating invocations.
- Route surface approved: page-scoped Reader state and progress, authenticated admitted PDF bytes, mentor exchange list/create/detail, server-observed source reads, structured proposal staging, bounded client-observed events, cancellation, and human-only decisions.
- The mandatory Spec interview beats are complete. The project owner accepted the recommended focused optional deepening pass over the three highest-risk live-demo boundaries before formal Spec generation.
- Spec deepening count: 1. The mandatory architecture interview and the one approved optional deepening round are complete.
- Deepening decision 1 — named-client truth gate: move minimum public HTTPS deployment into Gate 0, prove the native read/stage loop in the exact ChatGPT desktop tuple, and record separate text and visual client tuples/evidence modes instead of implying one client trace proves both.
- Deepening decision 2 — visual proof and custody: pre-key controlled A/B regions with crop-exclusive human ground truth, require a separate full-figure run, retain exact client-produced PNG bytes with server digest/admitted-document binding, and reopen them only through an authenticated source-item-scoped route.
- Deepening decision 3 — failure/accessibility truth: make registration snapshots explicitly client-asserted and persisted after exchange creation, treat callback delivery separately from model use, test client-version unregister behavior, and lock deterministic dialog/focus/error/nonvisual behavior into every slice.
- Created `docs/hackathon-build/spec.md` as the canonical implementation contract covering stack, topology, seven-record data model, closed DTOs, private artifact custody, API routes, WebMCP tools, agent boundaries, accessibility, security, verification, deployment, and checklist handoff.
- Current WebMCP reliability review shortened the canonical tool names to `paperpilot.read_sources` and `paperpilot.stage_explanation`, set annotations according to actual callback effects/output trust, and separated the durable 50,000-byte source ceiling from the named client's serialized tool-result ceiling. The longer names recorded earlier are historical interview output, not implementation requirements.
- Independent Spec audits covered PRD traceability, database/API/idempotency/storage closure, accessibility, prompt injection, exact supported-client semantics, and false-claim boundaries. Incorporated corrections include admitted-PDF generation binding, complete manifest/chunk FKs, HMAC read receipts, private artifact retrieval and quota accounting, replay-before-CAS decisions, per-source synthesis trails, intent-specific mentor validation, and safe persistent citation warnings.
- Technical Spec handoff: next guided stage is `build-checklist`.
- Final closure pass: three independent read-only auditors returned PASS after rechecking PRD acceptance/undefined DTOs, WebMCP and accessibility truth, and database/API/transaction/storage scope. The canonical Spec is 2,781 lines with SHA-256 `074EB1DDF4883D4DE92B96216F88EF639A217D4FB284D8F56BA9D7EB6B0CF662` at handoff.

## 2026-08-29 — Build Checklist planning

- The project owner chose the recommended hand-off path: Codex owns sequencing and executes autonomously rather than running a second co-design interview.
- Build cadence locked as a speed-run with focused automated checks after every item and only three participant look-at-it pauses: named-client truth after Gate 0, the complete exact-text vertical slice, and the complete visual vertical slice.
- The previously approved wow moment remains authoritative and was not re-asked: visible real WebMCP source-read/stage activity plus the resulting inspectable source → agent → human trail; figure-region understanding is supporting first-class proof rather than the sole hero.
- Initial Git safety note: the checkout first appeared as an untracked child of an enclosing worktree. Autonomous building could not stage or commit until PaperPilot was confirmed as an isolated repository boundary.
- Checklist deepening count: 0. The hand-off path skips a separate deepening interview; the project owner's required gut-check applies to the finished draft.
- Created `docs/hackathon-build/checklist.md` with 12 dependency-ordered work packages spanning Gate 0 truth spikes, the exact-text slice, the visual slice, same-paper teaching and hardening, release evidence, and the required Devpost handoff.
- Checklist status remains draft pending the project owner's gut-check; guided state does not advance to `build-project` until the owner accepts the amount and sequence of work.
- Three independent read-only checklist audits covered dependency order/scope, PRD and release-gate traceability, and WebMCP/accessibility/security boundaries. They found no orphaned PRD epic, release row, or no-compromise invariant.
- Audit correction incorporated: the repository does not yet define `test:e2e` or `demo:preflight`; the checklist now creates those scripts before they are used as required release commands.
- Sizing truth: the approved candidate cannot honestly fit twelve literal 15–30-minute tasks. The document therefore uses 12 dependency-ordered work packages and requires `$build-project` to split each into short internal green/red cycles without adding more participant pauses.
- The project owner approved the finished checklist without changes. The twelve-package order, autonomous speed-run mode, checkpoint-commit safety rule, and participant pauses after items 2, 8, and 10 are now locked for `$build-project`.
- Checklist handoff complete: the next guided command is `build-project`; implementation has not started during the checklist stage.

## 2026-08-29 — Guided build execution

- The project owner advanced with “Next,” authorizing execution of the approved autonomous checklist.
- Guided state entered `build`; checklist item 1 is the active work package.
- The build will use short internal green/red cycles, preserve existing work, and stop for participant inspection only after items 2, 8, and 10 unless a no-compromise invariant fails earlier.

## 2026-08-29 — Checklist item 1 implementation checkpoint (blocked at public Gate 0)

- Corrected the repository boundary before any checkpoint commit: the PaperPilot checkout became its own Git repository rather than an untracked child of an unrelated enclosing repository. At that point it had no commits, so item 1 remained unverified.
- Kept local mutable state on the `E:` drive. Applied the pending `20260829261000_user_name_text_policy` migration to the drive-scoped Prisma Dev database and restored the local database after stress runs; the local application readiness endpoint returned `200 {"status":"ready"}` at handoff.
- Added the single-host release skeleton: a multi-stage non-root application image, Caddy public/private TLS configuration, private PostgreSQL/validator/extractor/ClamAV networks, supervised validation and extraction workers, one shared private document volume, bounded resources/logs, explicit database role/migration jobs, a release environment contract, and a safe deployment/rollback runbook.
- Added a deny-by-default root `.dockerignore`. Local `.env` files, retained `.paperpilot-data`, Git metadata, dependencies, generated build output, reports, logs, and private-key formats are excluded from all three repository-root image contexts while the app, migrations, operations scripts, validator, extractor, and scanner configuration remain available.
- Corrected production worker commands to invoke the pinned local `tsx` binary without a developer `.env`; added a real ClamAV daemon ping healthcheck; passed the all-or-nothing transactional-email variables only to web; and kept ordinary worker egress closed.
- Added a phase-aware `demo:preflight` that checks repository/deployment files, immutable image/build inputs, exact Compose topology, private ports and networks, shared storage, supervised runtime state, authenticated private readiness, public health, and bounded release evidence. It explicitly leaves fresh upload behavior, native-client activity, visual understanding, and accessibility as `NOT CHECKED` instead of promoting metadata into proof.
- Fixed a Compose path-resolution false-positive: preflight now uses the directory containing `deploy/app/compose.yaml` as the Compose project directory, matching actual build-context and bind-mount semantics. The deployment runbook now invokes Compose from the repository root with an explicit `deploy/app/.env`.
- Added the minimal arbitrary admitted-PDF visual Reader slice with pinned `pdfjs-dist` `6.3.289`, an authenticated admitted-original endpoint, exact document/digest/ETag generation binding, server and client SHA-256 verification, post-read authority recheck, same-origin worker delivery, and an accessible page-one status/error/summary surface. The existing exact-text Reader remains available independently.
- Closed an audit defect in `PAPERPILOT_READER_PDFJS`: unset or `0` now disables both UI and binary route, exact `1` enables them, and every other value fails configuration closed. The public Gate 0 environment must explicitly enable the flag.
- Reconciled two stale integration assertions with existing authority contracts: document-link failures now expect the generalized “validated PDF source” message, and retained-PDF counts exclude storage-authority/tombstone control files.
- Automated verification after the final patches: `npm run lint` passed; `npm run typecheck` passed; `npm test` passed **635/635**; focused Reader-PDF tests passed **9/9**; demo-preflight tests passed **9/9**; and a production-shaped `npm run build` with `PAPERPILOT_READER_PDFJS=1` passed and emitted the Reader PDF route plus same-origin PDF.js worker.
- Static release verification passed with a SHA-verified standalone Docker Compose v2.40.3 configuration render and Caddy v2.10.2 validation. This proves syntax/topology only; this Windows host has no Docker daemon and those tools did not build or run the release containers.
- The broader local upload/link/Reader database suite improved to **15/17** after the stale assertions were corrected. Remaining failures are not claimed green: the Prisma pg adapter hit `SET TRANSACTION ISOLATION LEVEL must be called before any query` during a grounded-evidence replay, and the drive-scoped Prisma Dev process terminated during the final quota case. Existing Reader authorization, custody, unavailable-state, generation, cursor, and page-cap cases that executed passed. Target-host PostgreSQL verification remains required.
- `npm run demo:preflight -- --phase infrastructure` remains intentionally red: **2 passed, 3 failed, 5 blocked, 5 not checked**. There is no configured public HTTPS origin, release ID, populated `deploy/app/.env`, Docker runtime, or recorded signed-in fresh-PDF upload through both workers and PDF.js.
- Gate decision: checklist item 1 remains unchecked and item 2 has not started. Advancing would violate the approved rule against simulating public HTTPS, worker/shared-volume behavior, admitted-byte binding, or the named-client truth spike. The next build action requires a real Linux Docker host/domain/SSH path, or explicit approval for a lower-isolation temporary HTTPS tunnel used only for an isolated feasibility spike.

## 2026-08-29 — Local hosting and pgAdmin development checkpoint

- Active shaping moment: after rejecting paid VPS hosting and then declining the Sites runtime migration, the project owner directed: “Host it locally and use pgAdmin.”
- Kept the currently healthy Next.js application loopback-only at `http://127.0.0.1:3000`; `/livez` and `/readyz` both return `200`, and readiness includes a fresh database connection plus migration-sentinel verification.
- Verified that the active Prisma Dev database is the named `paperpilot` PGlite/PostgreSQL-compatible instance on `127.0.0.1:51218`, with its shadow database on `51219` and persistent state under the checkout-drive-scoped Prisma Dev root.
- Targeted checks found no Prisma data at the known old system-drive roots. Upload quarantine also remains under the repository's ignored `.paperpilot-data` directory.
- Installed pgAdmin 4 version 9.17 from the signed Windows Package Manager package, launched it as the visible database client, and registered the password-free `PaperPilot Local` profile against the existing database. pgAdmin did not create or relocate a second PaperPilot database.
- Verified PostgreSQL wire compatibility with pgAdmin's bundled `psql`: the connection resolved to database `template1`, user `postgres`, row security `on`, and 57 public application tables. No password was written to the repository or printed by the new preflight.
- Added stable local Prisma Dev control/direct/shadow ports (`51213`/`51218`/`51219`), a password-free pgAdmin server profile, `npm run dev:local`, `npm run local:check`, and the dedicated `deploy/local/README.md` lifecycle and recovery contract.
- This is a local development checkpoint, not a public release claim. It does not prove public HTTPS, production role separation, Docker isolation, validator/extractor supervision, shared-volume behavior, or judge access; checklist item 1 therefore remains unchecked.

## 2026-08-29 — Supabase project staging checkpoint

- Active shaping moment: the project owner supplied Supabase project reference `avmcmmayvnjxrhrmgsdx` and asked to hook it up.
- Verified only the public, non-secret routing boundary: the exact REST and Storage gateways identify that project and reject unauthenticated access, the direct database hostname resolves, and this workstation can establish a TCP connection to port `5432`.
- No Supabase access token, database password, Storage server credential, authenticated CLI session, provider CA file, or deploy-time database authority was present. No database role, migration, bucket, policy, or object was created or changed, and no authenticated-readiness claim was made.
- Added an opt-in direct-database profile fixed to `db.avmcmmayvnjxrhrmgsdx.supabase.co:5432/postgres`, runtime role `paperpilot_runtime`, and `sslmode=verify-full`. The profile rejects other projects, pooler substitution, the provider `postgres` login, missing passwords, extra query parameters, weaker TLS, and local-development fallback.
- A real unauthenticated PostgreSQL handshake exposed the expected provider-certificate trust gap on this machine. The profile therefore requires `PAPERPILOT_DATABASE_CA_CERT_PATH`, validates a bounded CA-only PEM file outside the repository, and supplies it explicitly to both Prisma and the readiness client with certificate verification enabled.
- Added `npm run supabase:check` as a credential-free, fail-closed endpoint preflight. Its output explicitly separates verified routing from database authentication, roles, migrations, Storage bucket configuration, and Storage credentials.
- Supabase's managed `postgres` role is not a true PostgreSQL superuser, and the provider profile targets the managed `postgres` database. The dedicated-cluster bootstrap in `deploy/postgres` was not run or weakened; a provider-specific role and migration reconciliation remains required before cutover.
- The E-drive Prisma Dev database remains the active application database and rollback boundary. Checklist item 1 remains unchecked because public deployment, authenticated managed-database readiness, full worker custody, and the named-client WebMCP proof are still unverified.
- Supabase Storage remains a later provider-adapter slice. A project reference alone cannot provision or authenticate a private bucket, and switching the current local PDF custody path without the validation, extraction, Reader, reconciliation, and digest-verification boundaries would create a false provenance claim.

## 2026-08-29 — No-local-database-write architecture amendment

- Active shaping moment: the project owner explicitly corrected the prior rollback assumption—“There shouldn’t be written anything to our local db.” This supersedes the writable Prisma Dev/pgAdmin workflow and makes Supabase project `avmcmmayvnjxrhrmgsdx` the only approved application database target.
- Immediately stopped the loopback Next.js process and used the repository-supported named-instance stop operation for Prisma Dev. Verification found no listeners on `3000`, `51213`, `51218`, or `51219`; the retained E-drive database files were not deleted or migrated.
- Removed the local database URL and shadow URL from the ignored `.env`, disabled the local compatibility flag, and selected the exact Supabase profile. With the CA and runtime credential still absent, PaperPilot now remains unavailable instead of falling back locally.
- Made the live Prisma client and `/readyz` require the exact Supabase project profile in every environment. Empty, generic, alternate-project, and loopback application configurations fail before network I/O; the old local pool/transaction branch was removed.
- Wrapped every supported web and worker start command in a no-socket Supabase configuration preflight. Disabled `db:dev`, `db:migrate`, `db:studio`, and ordinary `.env` integration tests; the Prisma Dev launcher accepts only the exact emergency stop command.
- Added a read-only freeze check that verifies the exact profile, rejects every configured local/unapproved authority and shadow database, and probes the retired IPv4/IPv6 listener ports without opening a database connection or writing a file.
- Hardened Prisma CLI configuration so offline client generation uses a non-secret exact-project sentinel while configured CLI targets must use the approved Supabase direct authority; local and shadow targets are rejected.
- The earlier self-hosted PostgreSQL Compose topology is now a known red gate and may not be used for release. Replacing it with CA-mounted Supabase connectivity and provider-specific migration authority remains inside checklist item 1; no authenticated Supabase or Storage readiness claim has been made.
- Checklist item 1 remains unchecked. The local-write freeze can be green while the application is deliberately offline; completion still requires authenticated remote role/schema/migration/readiness proof and the remaining public Gate 0 evidence.

## 2026-08-30 — No-local-database enforcement checkpoint

- Converted the active `deploy/app` topology to Supabase-only operation. Removed the PostgreSQL service, `postgres_data` and `postgres_tls` volumes, database-internal network, database leaf/key export, Caddy database listener, and all self-hosted database operation containers. Web and both database-backed workers now require the exact Supabase profile, a read-only provider-CA mount, and the shared non-internal `database_egress` network.
- Strengthened infrastructure preflight so it rejects any retired self-hosted PostgreSQL primitive, stale database container, generic/alternate database target, weak or malformed runtime URL, missing read-only CA mount, or database client without managed-database egress. Focused preflight tests pass **12/12** without printing connection URLs or passwords.
- Disabled all supported database lifecycle, migration, Studio, integration, deployment, role, smoke, ledger, and authority-snapshot commands until their Supabase-specific replacements are reviewed. Retained administration parsers are additionally pinned to the exact approved project before any socket can open.
- Closed the direct-CLI escape: `prisma.config.ts` now admits only offline `prisma generate`; direct Prisma Dev, migration, Studio, validation, and SQL-execution commands fail while loading project configuration. Database-dependent integration helpers also reject every non-approved URL before constructing their raw clients.
- Made Prisma client construction lazy, so unit tests and production builds that only import server modules do not parse database configuration, open a socket, or create a pool. The first actual database member access still performs the exact Supabase URL and CA validation.
- Expanded the no-local access proof to validate the Compose-facing database URL, check standard PostgreSQL port `5432` plus retired ports `51213`/`51218`/`51219` on IPv4 and IPv6 loopback, and fail closed on timeout, permission, or other indeterminate probe outcomes. Live verification returned `local_database_write_frozen`; no listeners existed on those ports or application port `3000`.
- Removed the checked-in pgAdmin registration and updated the root, local-archive, Supabase, deployment, PostgreSQL-authority, Zotero, scope, checklist, and environment documentation so no active path instructs developers to start or write the retained archive.
- Credential-free Supabase endpoint verification remains green for project `avmcmmayvnjxrhrmgsdx`: REST and Storage gateways reject unauthenticated access as expected, database DNS resolves, and TCP `5432` is reachable. Authentication, runtime role creation, migrations, private Storage, and authenticated readiness remain explicitly unverified.
- Final no-database verification passed: `npm run db:generate`; `npm run lint`; `npm run typecheck`; `npm test` **655/655**; `npm run build`; `npm run db:local:freeze-check`; and `npm run supabase:check`. Docker is not installed on this workstation, so native `docker compose config` and container execution were not claimed; the parsed topology and adversarial configuration tests are green.
- Checklist item 1 remains unchecked. PaperPilot is intentionally unavailable until the external Supabase CA and credentials are installed and the provider-specific role/schema/migration/readiness workflow passes; it never falls back to a local database.

## 2026-08-30 — Serverless-only production architecture amendment

- Active shaping moment: the project owner explicitly required, “I want this serverless.” This supersedes the Linux VPS, Caddy, Docker Compose release host, shared private volume, continuously supervised validation/extraction workers, persistent ClamAV daemon, and production-local-storage plan.
- The approved release topology is Vercel Next.js Functions for the authenticated control plane and WebMCP Reader, Vercel Workflow for durable orchestration, one fresh `persistent: false` Vercel Sandbox per PDF job attempt, Supabase PostgreSQL as the sole durable database, and private Supabase Storage as the sole durable PDF/artifact authority.
- Browser uploads and Reader downloads use short-lived, actor-authorized, exact-object capabilities. PDF and artifact bodies do not traverse Vercel Function or Workflow payloads. The Sandbox receives no database password or Supabase service-role credential, verifies input size/digest before parsing, and is stopped on every terminal path; every retry gets a new Sandbox identity.
- The Supabase-only and no-local-database invariants remain unchanged. Vercel runtime database calls require the exact project-scoped Supavisor transaction profile on port `6543`; migrations use a separately reviewed direct provider authority. No local or generic target becomes a fallback.
- Existing `deploy/app`, validator/extractor Compose files, polling entrypoints, and local-quarantine implementation are retained only as historical or local contract references while provider adapters replace them. They cannot satisfy Gate 0 or release preflight.
- This amendment does not add vector retrieval. Exact text/visual WebMCP handoff remains deterministic and source-set bound; optional Supabase pgvector discovery stays post-demo unless separately approved.
- Guided state remains `build`, checklist item 1 remains active, and the next internal cycles are provider contracts/mock lifecycle tests, direct private Storage transfer, Workflow/Sandbox execution, serverless database profile, Preview deployment, and real fresh-PDF proof.

## 2026-08-30 — Serverless Gate 0 implementation cycle 1

- Installed exact stable `workflow@4.8.5`, `@vercel/sandbox@3.2.1`, and development-only `@vercel/sandbox-mock@3.2.1`; wrapped the existing Next configuration with `workflow/next`. The production build now compiles the Workflow integration and emits Vercel's internal flow/step/webhook routes. No PDF Workflow is claimed yet.
- The first install exposed audited transitive `nanoid` and `undici` advisories. Added narrow version-specific overrides to `nanoid@5.1.16` and `undici@7.29.0`; `npm install` then reported **0 vulnerabilities**. No force upgrade or unrelated dependency mutation was used.
- Split upload configuration into a serverless control-plane policy and a legacy local-quarantine configuration. Authenticated upload-session reservation can now parse quotas/TTLs in production without demanding a local path; the existing local byte-proxy path still deliberately requires its old configuration until the signed Supabase adapter replaces it.
- Added the provider-neutral exact-PDF object contract: closed `LOCAL`/`SUPABASE_STORAGE` bindings, non-overloaded local authority versus provider object-version fields, bounded random-access reads, centralized exact byte-count/SHA-256 verification before consumer access, safe path-free failures, and provider-owned post-callback mutation checks.
- Wrapped the existing local quarantine behind that contract without weakening tenant/asset/key validation, root-generation fencing, held-handle reads, or post-operation file-identity checks. This is a transition/test adapter only; it is not an approved Vercel fallback.
- Added the provider-neutral disposable PDF Sandbox lifecycle and real Vercel factory. Launch input requires a digest-pinned image, 1–4 vCPUs, bounded 45-minute-or-less timeout, exact storage hostname, one job-attempt tag, and `persistent: false`. The lifecycle stops the Sandbox after success, failure, unexpected persistence, and exceptions, and fails a nominally successful attempt when termination cannot be confirmed.
- Added the canonical serverless ADR, amended Spec/Checklist/README/environment/provider docs, and tombstoned the single-host deploy runbook. The current Compose-oriented `demo:preflight` remains a known superseded implementation and cannot satisfy the new release Gate 0.
- Verification passed: focused new suites **17/17**; root suite **667/667**; validator **25/25**; extractor **29/29**; lint; typecheck; production Next build with Workflow routes; and `db:local:freeze-check`. Devpost readiness remains intentionally red at **24/40** because public URL, released WebMCP/selection/accessibility proof, tested-client metadata, demo video, and final submission evidence do not exist yet.

## 2026-08-30 — Serverless Gate 0 implementation cycle 1 safety review

- Bound each private Supabase PDF key to domain-separated hashes of its durable organization and asset identities; a tenant or asset swap now fails before bytes are exposed. The attempt portion remains an opaque hashed segment issued by PaperPilot, and provider object versions reject controls and oversized values.
- Combined outer-operation and body-specific cancellation for verified reads, so either authority can stop a bounded stream.
- Preserved application callback failures through the transitional local object-store adapter instead of misreporting them as provider outages.
- Hardened the disposable Sandbox boundary: malformed provider sessions fail closed, every valid returned session is stopped on success or failure, and an unconfirmed stop becomes the terminal `sandbox_stop_failed` outcome even if processing also failed. The primary processing error remains available only as an internal cause.
- Excluded Workflow's generated, nested-gitignored internal routes from project lint without excluding authored Workflow code.
- Verification after the review: serverless/config focused suites **20/20**; complete root suite **670/670**; lint with zero warnings; typecheck; `git diff --check`; and `npm audit` with **0** known vulnerabilities.
- Checklist item 1 remains unchecked. The next cycle is the exact Supavisor `:6543` runtime profile plus separate migration authority, Supabase private-bucket reserve/finalize capability adapter, client use of the returned transfer plan, and exact-generation Reader access descriptor. After that, implement the real PDF Workflow/Sandbox processor and Preview canaries.

## 2026-08-30 — Serverless Gate 0 implementation cycle 1 independent review correction

- A read-only independent review found that the first Vercel adapter wrapped the SDK `Sandbox` in a plain object with function properties. That value fails Workflow serialization even though Vercel's native class is supported. The adapter now returns the native `Sandbox` unchanged, and the official `@vercel/sandbox-mock` verifies safe launch arguments plus the native serializer hook.
- Removed the caller-supplied storage hostname from the lifecycle. Every Sandbox now starts with `networkPolicy: "deny-all"`. The lifecycle has structurally separate storage-staging and offline-processing callbacks, restores deny-all before offline processing, rechecks cancellation after create and staging, makes an unconfirmed air-gap a fixed failure, and attempts a final deny-all transition before stop on exceptional paths.
- Corrected the Storage secret design before implementation: browser upload and Reader download retain exact signed capabilities, but no signed URL or Storage JWT may enter a durable Sandbox command or Workflow value. Gate 0 now requires a server-only, job-fenced binder that loads an attempt-scoped Supabase Storage credential inside the server step and installs an exact configured-project host/path/method Vercel firewall transform outside the guest. The binder, JWT/RLS policy, and Workflow trace proof are still unimplemented and therefore remain red.
- Corrected Sandbox cleanup truth: `finally` covers ordinary application paths but is not assumed to run after external Workflow cancellation or platform termination. Gate 0 now requires durable job/Sandbox cleanup state plus a scheduled idempotent reconciler for tagged cancelled, expired, superseded, or terminal attempts; the bounded Sandbox timeout is only a last backstop.
- Corrected the Supabase role documentation. The checked-in `:5432` `paperpilot_runtime` parser is a legacy direct-runtime transition verifier, not migration/admin authority. Removed the unused `PAPERPILOT_MIGRATION_DATABASE_URL` placeholder and documented that the existing dedicated-cluster deploy scripts are not an approved Supabase migration path. Separate exact Supavisor runtime and Supabase-compatible migration profiles remain release blockers.
- Verification after correction: serverless/config focused suites **23/23**; complete root suite **673/673**; validator **25/25**; extractor **29/29**; database-role contracts **14/14**; lint with zero warnings; typecheck; production Next build; `git diff --check`; local-database write freeze; exact-project credential-free Supabase routing check; and `npm audit` with **0** known vulnerabilities. The build truthfully reports **0 authored PDF Workflows**. Devpost readiness remains intentionally red at **24/40** because the public release and product/native-client evidence do not exist yet.
- Checklist item 1 remains unchecked. The next implementation cycle is the exact Supavisor runtime and provider-specific migration authorities, followed by private bucket/RLS plus browser transfer and Sandbox binder contracts, exact Workflow job claims/replay, tagged cleanup reconciliation, and a real Vercel Preview canary.

## 2026-08-30 — Serverless Gate 0 implementation cycle 2: Supabase authority setup

- Replaced the retired direct application profile with an exact Supavisor transaction profile for project `avmcmmayvnjxrhrmgsdx`: dashboard-issued pooler host, port `6543`, database `postgres`, project-scoped `paperpilot_runtime.avmcmmayvnjxrhrmgsdx`, explicit password, `sslmode=verify-full`, and `pgbouncer=true`. Application and readiness traffic reject the direct endpoint and default to a one-connection serverless pool.
- Added non-interchangeable direct profiles for the one-time provider `postgres` bootstrap and `paperpilot_migration_owner` release authority. Runtime, migration, and bootstrap URLs reject each other's roles, ports, hosts, and profiles before I/O. Prisma now permits only offline `generate` and exact `migrate deploy`; `migrate dev`, reset, db push, Studio, shadows, generic hosts, and loopback targets stay blocked.
- Added a Supabase-managed role bootstrap that changes only `paperpilot_migration_owner` and `paperpilot_runtime`, takes both passwords from already validated URLs, binds them through PostgreSQL parameters without logging them, preserves managed provider authority, and never runs the dedicated-cluster bootstrap.
- Added manifest-scoped runtime grant reconciliation. It requires the exact 57-table authority, denies PaperPilot-owned objects to `PUBLIC`, `anon`, `authenticated`, and `service_role`, withholds `_prisma_migrations`, preserves the narrow retained-principal column grants, and grants only six reviewed functions. It does not transfer the `public` schema or rewrite provider database ACLs.
- Added `SUPABASE_STORAGE` to the Prisma provider enum and advanced `/readyz` to an enum-label catalog sentinel. Added exact private-bucket configuration for `paperpilot-private-pdfs`: private, PDF-only, and 25 MiB maximum. The server-only setup parser accepts only the supplied project and current `sb_secret_...` key shape and explicitly rejects legacy or `NEXT_PUBLIC_*` secret-key variables.
- Pinned `@supabase/supabase-js@2.112.4` and the Supabase CLI `2.116.0`. Added credential-safe `supabase:roles:bootstrap`, `db:deploy`, `supabase:roles:reconcile`, `supabase:storage:apply`, and `supabase:storage:check` commands. None starts a local Supabase stack, and the ignored local environment now selects the new transaction profile while leaving every missing credential empty.
- Credential-free project routing remains green, and the no-local-database freeze now reports the transaction profile with no configured application target. The authenticated Storage check correctly returns `supabase_private_storage_not_ready` because no server secret is installed. No remote database role, migration, bucket, policy, object, or Vercel secret was changed or claimed.
- An independent final review caught five pre-credential security defects. The corrected bootstrap now removes and verifies every inbound/outbound membership involving either PaperPilot role; reconciliation refuses attribute or membership drift; the migration owner uses `search_path=public` so `pg_catalog` retains implicit precedence; the pool budget is exactly one rather than accepting/silently defaulting arbitrary values; the migration wrapper rejects every forwarded option; and readiness proves an atomic grant sentinel (User CRUD, no migration-ledger read, and one reviewed function) in addition to the migration sentinel.
- Verification passed: Supabase setup suites **17/17**; root suite **678/678**; validator **25/25**; extractor **29/29**; Prisma generation; lint; typecheck; production Next build; `git diff --check`; exact-project public Supabase preflight; no-local write freeze; and `npm audit` with **0** known vulnerabilities. The build still reports **0 authored PDF Workflows**, and Devpost readiness remains truthfully red at **24/40**.
- Checklist item 1 remains unchecked. The next red gate is credentialed remote setup and live pooler/bucket verification, followed by the direct browser transfer/finalize adapter, Storage JWT/RLS plus Vercel firewall binder, authored PDF Workflow, orphan Sandbox reconciliation, Preview deployment, and a fresh arbitrary-PDF canary.

## 2026-08-30 — Serverless Gate 0 implementation cycle 3: direct PDF transfer contracts and custody guards

- The Supabase runtime, migration, bootstrap, and server-only Storage credentials remained empty. This cycle opened no database or Storage connection and changed no remote role, migration, bucket, policy, object, or Vercel setting.
- Added an exact attempt-bound Supabase object-key assertion. Organization and asset binding alone are no longer accepted: a key generated for another attempt of the same asset fails before capability minting, metadata lookup, cleanup, or byte verification.
- Added closed browser transfer contracts for transfer request, signed upload plan, finalize request, and private Reader access. They accept only project `avmcmmayvnjxrhrmgsdx`, bucket `paperpilot-private-pdfs`, canonical attempt-key paths, HTTPS, one bounded token, exact methods/headers, bounded canonical expiry, lowercase SHA-256, exact size, and the matching same-origin finalize route. Bucket, key, ETag, provider version, arbitrary headers, and capability material are rejected from client commands.
- Added the direct browser transport seam: same-origin JSON requests create and finalize the transfer; the raw PDF `File` is PUT directly to the signed Supabase URL with `withCredentials = false`, no cookie or application authorization, exact create-only headers, progress/cancellation, and exact provider `{ Key }` confirmation. Finalize sends only the closed attempt/size/digest command and strictly sanitizes the returned upload status. The existing `/content` upload remains explicitly legacy and the live UI has not yet been cut over.
- Browser reservation now computes SHA-256 before issuing the upload command. The command, request hash, durable session, audit, and provenance record carry that value explicitly as `browser-claim-until-sandbox-verification`; a changed digest conflicts under the existing idempotency boundary. The transition local adapter also rejects byte streams that do not reproduce the reserved claim. No browser digest is promoted into admitted `Asset` authority.
- Added a metadata-and-capability-only Supabase control plane using the pinned SDK. It exposes create-only upload capabilities, exact-object metadata, short current-path read capabilities, and version-checked exact deletion; it deliberately exposes no PDF body/read API. Returned URLs, paths, token, bucket, size, MIME, provider version, and safe errors fail closed against drift without leaking storage identity.
- Corrected a provider overclaim discovered during independent review. Supabase signed-upload tokens do not bind size, MIME, or SHA-256, and the pinned public SDK's signed GET does not expose provider-version pinning. Metadata finalize therefore remains `UPLOADING`; only a trusted Sandbox complete snapshot with matching before/after metadata, byte count, SHA-256, and PDF validation may transition to `QUARANTINED`. Unique keys are write-once, never recycled, and cleanup must wait until the fixed approximately two-hour provider write window plus safety margin.
- Added the separate atomic `20260830184500_supabase_storage_custody_guards` migration after the enum-only migration. It adds no columns: it freezes Supabase asset provider/bucket/object key from insertion, freezes admitted size/digest/ETag custody, freezes upload-attempt identity and committed observations, binds Supabase ingest receipts to the admitted asset ETag/protocol, and installs the exact private object-shape constraint as the new readiness sentinel.
- Verification passed: direct-transfer/control-plane/custody focused suites **134/134**; complete root suite **701/701**; Supabase setup/custody suite **22/22**; full lint; typecheck; production Next build; `git diff --check`; no-local-database freeze; and `npm audit` with **0** known vulnerabilities.
- Production build truth remains red where it should: it emits the legacy `/uploads/:id/content` and Reader PDF body-proxy routes, no transfer/finalize routes, and **0 authored PDF Workflows**. The direct HTTP client seam and database guards are ready, but server reservation/transfer/finalize cutover, immutable Sandbox snapshot/admission, Storage firewall binder, orphan reconciliation, credentialed remote migrations/bucket checks, Vercel Preview, and fresh arbitrary-PDF evidence are still required.
- Checklist item 1 remains unchecked. The next internal cycle is the credential-free server transfer/finalize state machine with injected Storage fakes, followed by the trusted Sandbox snapshot/Workflow path; the live Supabase/Vercel canary waits for the missing credentials and deployment authority.

## 2026-08-30 — Release-now WebMCP vertical slice

- Built a bounded public `/webmcp/` prototype that accepts an arbitrary born-digital PDF, renders its first page with PDF.js, extracts the embedded text layer, computes PDF/quote/source-set SHA-256 digests, and lets the reader freeze an exact bounded passage. Textless/scanned files fail honestly rather than fabricating source text.
- Registered exactly two canonical browser tools with `document.modelContext.registerTool`: `paperpilot.read_sources` returns only the currently frozen source, and `paperpilot.stage_explanation` validates and stages a structured research-mentor response. There is intentionally no agent-callable Save, Discard, Approve, or Verify tool.
- Added a visible source → WebMCP → human evidence trail generated inside the real callbacks. Each read/stage event records a random receipt/proposal identity and the relevant digests. Save and Discard remain ordinary human UI controls; the browser-local note and downloadable receipt are explicitly labeled as prototype persistence.
- Witnessed an actual Codex agent discover and call both tools through the in-app browser WebMCP capability, first on localhost and then at a public HTTPS Vercel preview. The public execution used an unrelated 15-page scientific PDF, returned one frozen 212-word source, and staged a seven-part mentor card without automatically saving or claiming scientific verification.
- Added a deny-by-default Vercel upload allowlist, a durable GitHub Pages deployment workflow for the static prototype, an MIT/public-repository release audit, a sanitized live-proof record, and a timed 2:30 demo-video plan with truthful claim boundaries and a backup recording path.
- Verification for the combined worktree passed: JavaScript syntax check for the public tool adapter; full lint; TypeScript; root tests **701/701**; production build; `npm audit` with **0 vulnerabilities**; and targeted secret scans. The authenticated Supabase/Workflow/Sandbox service is still intentionally not called production-ready.
- Formal guided checklist item 1 remains unchecked because credentialed Supabase roles/migrations/private Storage, the direct transfer routes, an authored PDF Workflow, disposable Sandbox canary, durable authenticated mentor records, and final accessibility/client evidence remain open. This release slice satisfies the immediate public WebMCP demo gates without weakening those fail-closed requirements.
- After the durable Pages rerun, the machine-readable Devpost manifest now records the anonymous HTTPS URL, released adapter, successful bounded read/stage verification, and exact tested Codex desktop build. `npm run devpost:check` advances truthfully from **24/40** to **30/40**; the remaining ten controls are the cross-PDF/selection matrix, activity failure states, keyboard/screen-reader proof, non-WebMCP fallback proof, public YouTube video plus duration/audio, immutable freeze, and final human submission confirmation.

## 2026-08-30 — Graph-first spatial Reader rebaseline

- Active shaping moment: the project owner made the new priority explicit—“We need the graph portion, the current WebMCP implementation is not good, and we really need to get moving on the knowledge graph and pdf annotation features.” The owner also required the paper in the middle, removal of the visible transcript, an automatic whole-paper map, agent-created/deleted nodes through WebMCP, a soft human check through Undo/Redo, and no PDF export.
- Reframed the public hackathon slice around a central multi-page PDF, a left research-mentor surface, and a right Graph/Evidence rail. The PDF retains a positioned selectable text layer and PaperPilot annotation overlays, but there is no separate transcript window.
- Distinguished automatic structural coverage from semantic interpretation. Every paper receives a paper-root plus outline/heading nodes or deterministic page-range fallbacks; semantic `main_idea`, `concept`, `method`, `result`, figure, equation, and prerequisite nodes require paper anchors and an explicit authority label.
- Selected Graphology as the in-memory graph authority and Sigma as a renderer, with an equal first-class accessible DOM outline. The graph uses stable explicit IDs and typed, source-grounded nodes/edges; current commands reject cross-paper anchors while preserving future-ready document-qualified IDs.
- Expanded the target WebMCP surface to six capabilities: read current focus, read graph, stage explanation, apply graph commands, apply annotation commands, and focus source. A named-client spike must freeze the final schemas before UI expansion; consolidation is allowed only if it preserves every capability.
- Locked the soft-check interpretation: valid agent graph/annotation commands apply immediately through one trusted, atomic reducer and remain human-reversible with Undo/Redo. Delete is a reversible tombstone, not hard deletion. Explanations continue to stage separately for explicit Save/Discard review.
- Audited `highkite/pdfAnnotate` at commit `b5e5bc2a4947d604610d15d78f47289074a0f2b7` and npm package `annotpdf@1.0.15` (MIT). It writes annotations into PDF bytes and is not a PDF viewer. Because export and byte mutation are excluded, it has no runtime/build role in this release; PaperPilot instead owns a PDF-space rect/quad anchor contract that can translate to a byte writer later.
- Rewrote the canonical Scope, PRD, Spec, and build checklist around the approved redesign. The guided workflow remains at `build`; this is a rebaseline of the active development loop, not a restart or a claim that the features are already implemented.
- Closed the pre-build contract gaps found in an independent audit: graph and annotation changes now share a workspace revision/inverse ledger; model command DTOs exclude trusted paper/origin/identity fields; caller-visible idempotency keys make retries reproducible; semantic digests exclude lifecycle metadata so Undo/Redo can reproduce prior/post state; structural nodes carry navigable page coverage; external citations are explicitly declared/unverified; snapshot quota failure is distinct from mandatory reducer failure; and the named-client spike must record the real visual evidence mode.
- Preserved the deployed two-tool `/webmcp/` release and its proof as historical evidence. It cannot satisfy the redesigned graph, spatial annotation, navigation, reversibility, accessibility, or no-export gates.
- First active build item: prove the named-client tool/dependency contract, then modularize the public slice, center the PDF, and establish spatial anchors before graph and mutation work proceeds.

## 2026-08-30 — Redesign checklist item 1 candidate: six-capability WebMCP truth spike

- Built a removable, localhost-only `spikes/webmcp-contract/` diagnostic instead of mutating the historical two-tool release. It holds one in-memory paper, exact-text and visual anchors, a Graphology directed multigraph, Sigma plus an equal accessible outline, reversible graph/annotation commands, activity receipts, human-only Undo/disposal, and a sealed visual A/B fixture. It performs no database, browser-storage, private-PDF, or remote-network I/O.
- Pinned `graphology@0.26.0` and core `sigma@3.0.3` beside the existing exact `pdfjs-dist@6.3.289`; `annotpdf`, React Sigma, PDF writing, and PDF export remain absent. The isolated server exposes only the spike and two exact same-origin vendor bundles.
- Froze six recursively closed input schemas and six locally validated result/error schemas for `paperpilot.read_focus`, `paperpilot.read_graph`, `paperpilot.stage_explain`, `paperpilot.apply_graph`, `paperpilot.apply_annotation`, and `paperpilot.focus_source`. Trusted paper identity, raw input geometry, human decisions, Undo/Redo, hard deletion, export, and cross-paper authority remain page-owned.
- Added a serialized mutation queue and replay-first idempotency check. Concurrent duplicate commands now create one semantic revision; an identical retry gets a fresh callback receipt tied to the original operation/revision, while changed content conflicts. Failed and foreign-paper batches roll back atomically; human Undo restores the prior semantic digest while advancing the audit revision.
- The first real-client pass found a missing read-after-write field: annotation mutation required a sub-digest the reads did not expose. Both read contracts now return `annotationDigest`; focused tests and the complete client flow were rerun after the correction.
- The Codex In-app Browser registered all six definitions twice across a hard reload and produced real receipts for focus read, graph read, source navigation, explanation staging, reversible graph mutation, graph reread, reversible annotation mutation, and idempotent replay. The page visibly advanced revision 1→3, grew from 3/2 to 4/3 nodes/edges, rendered `Weighted context`, and showed its exact-anchor annotation. A pre-reload handle was rejected as stale after replacement.
- A real `read_graph` result of 57,407 UTF-8 bytes containing 54 nodes and 3 edges arrived intact with `truncated: false`. Because the client became unavailable before a larger probe and no transport boundary is claimed, the frozen release ceiling is conservatively 48 KiB, leaving 8,255 bytes below the largest completed delivery. Input remains capped at 32 KiB canonical UTF-8 JSON.
- The visual locator flow succeeded, but screenshot capture timed out before a blinded canvas answer could be committed and revealed. The sealed pattern descriptors were absent from DOM/tool text, no human proof control was used, and the contract remains truthfully `locator_only` with `pixelUseVerified: false`.
- Verification passed: focused WebMCP suite **14/14**; exact Node syntax checks; production Next.js build; isolated server route/traversal/header smoke; and `git diff --check`. Checklist item 1 remains unchecked pending owner Verification Pause 1, including manual inspection of the activity trail and human-only Dispose control. The candidate record is `docs/release/WEBMCP-CONTRACT-SPIKE.md`.

## 2026-08-30 — Verification Pause 1 rejected: owner-feedback correction cycle

- The project owner rejected the first Verification Pause 1. When the owner inspected the spike, the annotation created during the earlier live WebMCP session was no longer present, and no spatial callback marker showed where the observed agent action applied in the paper or graph.
- The disappearing annotation exposed a verification-experience gap rather than durable persistence: the disposable spike intentionally keeps live graph mutations, annotations, replay keys, callback receipts, and history in page memory, so reload or a replacement tab creates a fresh session. Earlier receipts remain valid evidence of their original session but cannot substitute for a reviewable current page.
- The correction contract separates two state classes. A deterministic demo fixture will hydrate the same clearly labeled sample paper, graph addition, and source-linked annotation on every fresh demo load. This fixture is review scaffolding, not restored agent work or user persistence. Real WebMCP mutations remain separately labeled, session-only changes and must reset honestly on reload.
- The correction also requires a callback-driven provenance pointer. Each observed structured callback must identify its tool, status, callback receipt, and page-owned source, graph, or annotation target, then place a visible marker or focus ring at that target. The marker is evidence of a callback PaperPilot observed; it is not eye tracking, a remote mouse cursor, model intention, or scientific verification.
- Focus and reasoning claims are now explicit. `focus_source` may scroll and move DOM/keyboard focus only after a validated navigation callback. Read, explanation, mutation, and replay callbacks may update their visible pointer without stealing keyboard focus. PaperPilot will report no hidden chain of thought and will not infer why the model chose an action.
- Fresh correction evidence is now recorded from the Codex In-app Browser at `http://127.0.0.1:4175/`. The revised page registered 6/6 tools and loaded revision 1 with exactly one labeled fixture annotation, a visible on-paper chip, and the provenance cursor ready; browser diagnostics showed no warnings or errors.
- Actual correction-run callbacks were `callback:7678529f-0350-4537-986e-0128e3e75a4f` (`read_focus`, `ready`), `callback:ab390580-0c89-4fa7-b49b-2e08a1053c6d` (`read_graph`, `ready`), and `callback:940593c7-8fdf-4c6e-8ad8-3126011064a9` (`apply_annotation`, `applied_reversible`). The annotation advanced revision 1→2 and added `Agent edit — direct attention replaces recurrence`; the paper then showed the fixture and live-session chips plus a parked provenance pointer.
- The human visual-replay control re-presented the last callback target without changing revision 2. A separate fresh tab returned to revision 1 with exactly the labeled fixture annotation and without the live agent edit, directly confirming deterministic demo hydration versus session-only mutation rather than durable persistence.
- Correction verification passed: focused WebMCP contracts **16/16**; Node syntax checks for contract, app, test, and server files **4/4**; lint with zero warnings; typecheck; production Next.js build; root tests **701/701**; `git diff --check`; and isolated runtime checks for root 200 with CSP/no-store, both exact vendors, traversal 404, and POST 405. Devpost readiness remains intentionally red at **42/66**, with **24** open controls.
- The non-navigation focus policy now has a fresh manual client trial. Human **Focus region A** first placed keyboard focus on `visual-region-a`; `paperpilot.focus_source` then returned `callback:924c234e-1295-465b-8bcd-c0bd900df6e2` for `anchor:text:attention`, moved the visible provenance pointer, and left keyboard focus on `visual-region-a`. Browser warning/error diagnostics remained empty. Repeated owner inspection and explicit owner approval remain **pending**. Checklist item 1 stays unchecked, and no gate checkpoint is authorized until the owner passes the repeated Verification Pause 1.

## 2026-08-30 — Exact-paper correction after the second owner rejection

- The owner correctly rejected the central HTML facsimile: it was not the exact publication. The candidate spike now displays the official `Attention Is All You Need` arXiv v7 PDF through pinned PDF.js 6.3.289, with the real title, authors, abstract, arXiv marker, all 15 pages, and the actual Transformer Figure 1 on page 3.
- Added a local-only PDF source boundary. The ignored bytes must be exactly 2,215,244 bytes with SHA-256 `bdfaa68d8984f0dc02beaca527b76f207d99b666d31d1da728ee0728182df697`. A tracked source manifest records arXiv/DOI/authors/license, the independently computed body digest, and arXiv's non-matching transport ETag as a separate observation. Explicit fetch and verify commands reproduce the fixture without implying that the paper is covered by PaperPilot's MIT license.
- Removed the fabricated paper DOM and detached quote card. The center now has a real PDF.js canvas plus selectable in-place TextLayer; the synthetic A/B proof remains only as a collapsed developer diagnostic explicitly separate from the paper.
- The page resolves one verbatim page-1 sentence across PDF.js text spans with a DOM `Range`, merges duplicate span boxes into three real line rectangles, converts those rectangles to PDF-space quads, and mints the contract anchor digest from exact document identity and page-owned geometry. The agent cannot supply coordinates or coordinate aliases.
- Fixed three integration defects found only through browser verification: an early `ResizeObserver` could cancel the first anchor render before WebMCP registration; deferred page-input handlers read a cleared `event.currentTarget`; and SVG `hidden` did not suppress page-1 highlights on page 3. Initialization now resolves the exact source before observing resize, page values are captured synchronously, and non-source pages force `display: none` on the provenance SVG.
- Fixed PDF.js scale alignment by setting `--scale-factor`, `--user-unit`, `--total-scale-factor`, and scale-round variables on the page surface. A live 136%→151% check kept normalized highlight position/width stable within sub-pixel text-layout rounding, while the cursor remained attached to the same source union.
- The named In-app Browser registered all six WebMCP tools only after exact-byte, page-count, exact-text, and geometry verification. `read_focus` returned the real filename, 15-page count, body digest, verbatim sentence, three normalized line rectangles, and page-owned anchor digest. Browser warnings/errors remained empty.
- A live `apply_annotation` callback `callback:b6533ce1-5d1c-42bc-b73a-c18b36c4efdb` advanced revision 1→2 and visibly parked the provenance pencil on the highlighted sentence. Human Undo restored fixture-only revision 3; callback `callback:8f6aeaec-f829-4832-98ea-02970ea79588` then created a fresh visible reversible question at revision 4 for owner inspection. No database or browser storage was written.
- Focused contract/PDF-viewer verification is now **22/22**. Local source verification, Node syntax, live PDF/PDF.js/font/CMap/WASM route checks, traversal rejection, page 3 navigation, page-isolated highlighting, zoom reprojection, WebMCP calls, reversible annotation, and Human Undo all passed. Checklist item 1 remains unchecked and no checkpoint commit is authorized until the owner explicitly approves the repeated Verification Pause 1.

## 2026-08-30 — Verification Pause 1 owner approval and next-loop direction

- The project owner closed the repeated first verification pause by stating: “Now that we have the first iteration done, I want you to work on the annotation to graph functionality.” Under the guided-build natural-advance rule, this is explicit approval to freeze the six-capability exact-paper truth spike and continue.
- Checklist item 1 is now complete. The approved evidence remains the exact arXiv v7 PDF, real PDF.js source geometry, six registered WebMCP capabilities, observed callbacks, reversible graph/annotation mutation, a page-owned provenance pointer, and clean focused/browser verification.
- Active shaping moment: the reader—not the agent—must originate the annotation. A user-created annotation should become or link to a reader-authored, source-grounded graph node that the WebMCP API can inspect and later relate, update, or tombstone without gaining raw-geometry authority.
- Active shaping moment: `paperpilot.read_graph` needs bounded graph search in addition to overview/focus/neighborhood reads, while `paperpilot.apply_graph` remains the agent mutation path for node creation and revisioned edits.
- Active shaping moment: replace the page-at-a-time reading model with continuous vertical PDF scroll, active-page tracking, direct page-location controls, and source navigation that scrolls to the real page region. This requirement refines checklist item 3 without reintroducing a transcript or PDF export.
- The item-1 checkpoint commit is intentionally deferred until the concurrent viewer/contract edits settle; this avoids capturing partially written shared files. The next safe checkpoint will stage only reviewed PaperPilot build files and will exclude unrelated user artifacts.

## 2026-08-31 — Reader annotation graph and continuous-document iteration

- Replaced page replacement with one continuous 15-page vertical PDF reading surface. All page shells preserve scroll geometry; page 1 remains pinned for the approved exact-source evidence, while only the active page and its two nearest neighbors on either side retain expensive canvas/text rendering. The page locator now scrolls to a page instead of swapping the document, natural scrolling updates the active-page indicator, and zoom reprojects every page plus normalized provenance overlays.
- Added a page-owned user-selection seam. A valid single-page PDF.js text selection is captured as immutable exact text, SHA-256, page index, page view box, rotation, normalized line rectangles, PDF quads, and verified document identity. Cross-page, collapsed, detached, stale, oversized, foreign-document, and invalid-geometry selections fail closed.
- Added an atomic reader annotation transaction outside the WebMCP schemas. The page mints the anchor, then creates the reader annotation, a `reader_authored` graph node, and its `appears_in` provenance edge in one human-authored reversible revision. Human Undo removes the anchor, annotation, node, and edge together and reproduces the prior semantic digest. WebMCP cannot mutate the reader’s annotation body or supply its raw geometry.
- Extended `paperpilot.read_graph` with deterministic bounded search over node labels and summaries. Search is literal, Unicode/whitespace normalized, case-insensitive, control-character rejecting, filterable by node kind and authority, and stably ranked as exact label, label prefix, label substring, then summary substring. `paperpilot.apply_graph` remains the existing reversible agent path for source-grounded node creation and edits.
- Added the visible reader composer and human graph search UI beside the accessible Graphology/Sigma representation. A pending selection is visibly previewed on its owning PDF page, the reader supplies the annotation label and node kind, and the resulting node/annotation retain direct source navigation. No transcript, PDF export, browser storage, service worker, local database, or remote persistence was added.
- Live In-app Browser verification confirmed 15 ordered page surfaces, bounded rendering (`1, 8, 9, 10, 11, 12` around page 10), direct page-10 navigation with real selectable text, active-page updates, source focus back to page 1, and six registered WebMCP tools. `paperpilot.read_graph` search found the baseline attention concept; `paperpilot.apply_graph` created `Parallelizable attention` at revision 2 with `anchor:text:attention`; WebMCP search and the human search UI both found the new node. Browser diagnostics remained free of warnings and errors.
- The automation client intentionally strips native `Selection` mutation methods, so it could not synthesize a noncollapsed drag selection for the final physical gesture. The browser-visible text layer, overlay path, capture validation, and application integration are present; contract/viewer tests prove the complete trusted payload and atomic transaction boundary. A real human drag remains the short manual acceptance check before promoting this seam beyond the disposable spike.
- Verification passed: focused WebMCP/PDF suite **31/31**; exact-paper byte/page/digest verification; ESLint with zero warnings; TypeScript; production Next.js build; `git diff --check`; and the live WebMCP create/search receipts above.

## 2026-08-31 — Presentation-only annotation and graph arrangement iteration

- Active shaping moment: the owner asked to create annotations from the centered PDF, drag and rearrange their representations in the graph/annotation rail, and preserve WebMCP knowledge-graph behavior. The source boundary is explicit: the immutable PDF anchor never moves; only annotation-card order and linked-node display coordinates can change.
- Added a dedicated annotation-card drag grip, pointer card reordering, annotation-to-graph drop placement, direct Sigma node drag, accessible outline selection, four viewport-consistent nudge controls, focus-preserving **Move earlier** / **Move later** controls, reset, live announcements, and visible copy that distinguishes view arrangement from evidence changes.
- Kept presentation state outside canonical records. Annotation order is a module-owned ID array; graph layout is a node-keyed position map reconciled across Graphology replacement; Sigma selection/drag highlighting exists only in `nodeReducer`. No `order`, `selected`, `dragged`, or `highlighted` attribute enters Graphology, annotation projection, provenance, or WebMCP output.
- Added pure deterministic layout helpers and regression coverage. The contract proof moves `x`/`y`, reverses an external annotation order, preserves revision plus workspace/graph/annotation digests and exact source geometry, asserts `read_graph` leaks no view fields, applies the same anchored WebMCP graph mutation to moved/control states, and produces equal semantic digests plus a searchable new concept.
- Live In-app Browser verification selected and nudged `Attention replaces recurrence` while revision/digest text stayed unchanged, called `paperpilot.read_graph` with no layout leakage, then successfully created a second linked annotation through `paperpilot.apply_annotation` and reordered it without another semantic revision. A later `paperpilot.apply_graph` call rebuilt the Graphology/Sigma projection, created and searched `Layout-safe provenance`, preserved the selected layout controls, and still exposed no presentation fields. A responsive two-column overlap was also corrected so the graph rail cannot be covered by the sticky mentor rail.
- This is a tested shaping increment toward checklist item 6, not a claim that item 6 or the full public build is complete. Formal graph-to-PDF synchronization, broad cross-PDF validation, and the public bundle port remain open.
