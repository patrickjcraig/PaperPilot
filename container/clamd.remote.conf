# clamdscan client configuration for the private Compose scan network.
# The Clam daemon port must never be published outside that network: the
# protocol has no transport encryption or client authentication.
TCPAddr clamav
TCPSocket 3310
